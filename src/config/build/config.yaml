# ==============================================================================
# BUILD OVERRIDES
# ==============================================================================
# To nspect the render with a local server; changes port accordingly
baseURL: http://localhost:1337/ 

enableGitInfo: false
verbose: true
buildDrafts: true
buildFuture: true
publishDir: "server/build"