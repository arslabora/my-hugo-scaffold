{
  "name": "liuteriadautore-website",
  "version": "1.0.0",
  "description": "Source project which manage the developing, build and deploy of liuteriadautore.com website",
  "main": "index.js",
  "scripts": {
    "test": "npx hugo check & npx hugo --renderToMemory",
    "hugo:serve": "npm run hugo:s",
    "h:s": "npx hugo server -D",
    "hugo:build": "npm run hugo:b",
    "h:b": "npx hugo -e development --cleanDestinationDir -D -F",    
    "hugo:deploy": "npm run hugo:d",
    "h:d": "npx hugo -e staging --cleanDestinationDir --gc",    
    "hugo:publish": "npm run hugo:p",
    "h:p": "npx hugo -e production --cleanDestinationDir --gc --enableGitInfo --minify",    
    "clean:build": "rm -rf build/*",
    "clean:deploy": "rm -rf deploy/*",
    "clean:publish": "rm -rf publish/*",
    "clean:all": "rm -rf resources/* & npm run clean:build & npm run clean:deploy & npm run clean:publish",
    "publish": "echo 'commit the build and push to github. Or any other publish command...'"
  },
  "author": "Andrea Spada",
  "license": "ISC",
  "devDependencies": {
    "hugo-extended": "^0.80.0",
    "parallelshell": "^3.0.2"
  }
}
